"""Sinh dữ liệu biểu mẫu giả lập (synthetic) + ground-truth để chạy thử end-to-end.

Xem `generator.generate_all()`. Phụ thuộc: reportlab, Pillow, opencv, numpy
(import lười bên trong hàm nên import package này không cần các lib đó).
"""
